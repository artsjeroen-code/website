# Kopieer dit bestand op de Raspberry Pi naar:
#   /etc/rittenregistratie-auth.env
# en zet rechten op 600.
#
# Genereer een PBKDF2-SHA256 hash met bijvoorbeeld:
# python3 - <<'PY'
# import base64, getpass, hashlib, secrets
# password = getpass.getpass('Nieuw wachtwoord: ').encode()
# salt = secrets.token_bytes(16)
# iterations = 310000
# digest = hashlib.pbkdf2_hmac('sha256', password, salt, iterations)
# enc = lambda b: base64.urlsafe_b64encode(b).rstrip(b'=').decode()
# print(f'RITTEN_AUTH_PASSWORD_HASH=pbkdf2_sha256${iterations}${enc(salt)}${enc(digest)}')
# PY
#
# Plak alleen de gegenereerde regel hieronder/in het echte env-bestand.
RITTEN_AUTH_PASSWORD_HASH=
